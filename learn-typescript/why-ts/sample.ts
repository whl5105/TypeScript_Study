function add(a:number,b:number):number {
  return a + b;
}
let result = add(10, 20);
result.toLocaleString();
console.log(result);

/*
  a,b,return(빈환값) 모두 타입을 지정해줄 수 있다. 
  에러의 사전방지 
  타입에서 제공되는 api들을 활용 할 수 있다. 

*/